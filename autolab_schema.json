{
    "type": "object",
    "properties": {
      "variables": {
        "type": "object",
        "properties": {
          "vid_input_path": { "type": "string" },
          "vid_convert_dir": { "type": "string" },
          "transcript_path": {"type": "string"},
          "project_id": { "type": "string" },
          "recognizer_id": { "type": "string" },
          "instr_path": { "type": "string" },
          "model": { "type": "string" }
        },
        "required": ["vid_input_path", "vid_convert_dir", "transcript_path", "project_id", "recognizer_id", "instr_path", "model"]
      }
    },
    "required": ["variables"]
  }
  